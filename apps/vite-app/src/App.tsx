import { useState } from "react";

function App() {
    const [count, setCount] = useState(0);
    let test;

    if (test) return <p>hello</p>;

    return (
        <div>
            <h1>React App</h1>
        </div>
    );
}

export default App;
